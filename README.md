# chase_game
[!NOTE] em inglês pra charme 😁

## como rodar

```terminal
cmake --build build
```
o `main.cpp` está localizado em `./src/`
e o build vai para `./build/`

Então:
```terminal
./build/main
```
